type: array
description: |
  A list of data source identifiers. A data source is the system or provider
  where an entity originates (e.g., 'dataSource_Senate').
items:
  type: string
uniqueItems: true
example: ["dataSource_A"]
