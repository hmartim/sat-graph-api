get:
  tags:
    - "Introspection & Metadata Actions"
  summary: "Gets the list of all supported action types."
  operationId: "getSupportedActionTypes"
  description: |
    Returns the Action-type values recognized by the deployment's graph ontology.

    ### Discussion
    This action provides the canonical list of values that can be used in the `actionTypes` filter of other functions like `getItemHistory`. It allows a client application to dynamically build filtering interfaces without hardcoding ontology-specific values.
  x-sat-graph-agent:
    version: 1
    when-to-use: >-
      Discover the canonical Action-type vocabulary before building a causal
      query.
    pairs-with: [queryActions, getItemHistory]
    role: introspection

  security:
    - ApiKeyAuth: []

  responses:
    '200':
      description: "A list of supported action type strings."
      content:
        application/json:
          schema:
            type: array
            items:
              type: string
              example: "INSERTION"
            example: ["INSERTION", "TEXT_CHANGE", "REVOCATION"]
    '401':
      $ref: '../../openapi.yaml#/components/responses/Unauthorized'
    '403':
      $ref: '../../openapi.yaml#/components/responses/Forbidden'
